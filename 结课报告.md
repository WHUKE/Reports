# 知识图谱大作业报告

---

## 1 小组分工

### 1.1 各成员任务及所对应的报告小节标题或页码

| 成员姓名 | 任务描述                                                     |    对应报告小节/页码     |
| :------: | :----------------------------------------------------------- | :----------------------: |
|  李嘉轩  | JD 数据源扩展、反爬与清洗前处理                              |      第X节 / 第X页       |
|  陈宇轩  | JDParser 核心模块维护、解析效果数据统计、正则规则补强、新字段模型与解析扩展 |      第X节 / 第X页       |
|  谢羿衡  | 技能归一化字典扩充、LLM 兜底解析通道开发、增量解析文件交付、单测与文档迭代 | 2.2.2节、2.3.1节 / 第X页 |
|  刘义群  | JDGraphBuilder 清洗/校验层、proficiency 回填                 |      2.6节 / 第X页       |
|  汤语涵  | Schema、批量导入、增量更新、Neo4j 运维                       |      第X节 / 第X页       |
|  唐熙雷  | 查询 API、统计模块、缓存、技能层级规则                       |      第X节 / 第X页       |
|  邢泽雷  | JDGraphMono（图可视化 + 交互查询页面）                       | 第2.5.1、2.5.2节 / 第X页 |

### 1.2 小组贡献占比及各成员电子签

| 成员姓名 | 贡献占比 |         电子签名          |
| :------: | :------: | :-----------------------: |
|  李嘉轩  |   14%    |       （签名图片）        |
|  陈宇轩  |   14%    |       （签名图片）        |
|  谢羿衡  |   14%    | ![](../img/xieyiheng.png) |
|  刘义群  |   14%    |       （签名图片）        |
|  汤语涵  |   14%    |       （签名图片）        |
|  唐熙雷  |   14%    |       （签名图片）        |
|  邢泽雷  |   14%    |       （签名图片）        |
| **合计** | **100%** |          <br />           |

### 1.3 小组项目仓库地址

- 仓库链接：https://github.com/WHUKE/JDParser https://github.com/WHUKE/JDGraphBuilder https://github.com/WHUKE/JDGraphMono

---

## 2 知识图谱构建

### 2.1 数据源、获取方法和原始数据统计信息 (李嘉轩)

本项目的数据源主要来自国内主流企业公开招聘网站，包括腾讯招聘、百度招聘、美团招聘和京东招聘。为了提升岗位样本的代表性与时效性，实现了面向多站点的岗位爬虫模块，自动抓取公开岗位信息并统一整理为原始文本数据，作为后续实体抽取与知识图谱构建的输入。

**数据源与获取范围。**\
项目当前接入了 4 个招聘站点：腾讯、百度、美团、京东。各站点均优先抓取技术类岗位，以保证数据主题与“职业技能知识图谱”建设目标一致。具体来说，腾讯站点通过岗位类别参数 `parentCategoryId=40001` 过滤技术岗位；百度站点通过 `postType=1` 和 `recruitType=SOCIAL` 抓取社会招聘中的技术岗位；美团站点通过 `jfJgList` 与 `jobType` 等筛选参数限定技术岗位；京东站点则通过 `jobTypeJson=["YANFA"]` 聚焦研发类岗位。

从抓取流程上看，各站点遵循统一的处理链路：

1. 按页调用岗位列表接口，获取岗位概要信息；
2. 从列表结果中提取岗位 ID；
3. 若站点提供独立详情接口，则继续根据岗位 ID 请求详情数据；
4. 将不同站点返回的异构字段统一映射为内部的 `RawJobPosting` 对象；
5. 再格式化为统一风格的原始文本 `.txt` 文件写入 `data/raw/<site>/`；
6. 同时将抓取状态、岗位 ID、来源 URL、文件名、抓取时间、页码等信息追加记录到 `manifest.jsonl`，便于后续追踪、去重和失败重试。

其中，腾讯和美团采用“列表接口 + 详情接口”的两段式抓取方式：先通过列表接口获取岗位 `PostId` 或 `jobUnionId`，再请求详情接口获得完整 JD 内容；百度和京东的列表接口已返回较完整的岗位字段，因此可以直接将列表结果整理为原始文本并落盘。该设计减少了站点差异对后续解析模块的影响，使下游只需要面对统一格式的原始 JD 文本。

**原始数据统计。**\
根据当前仓库中的原始数据目录统计，项目共保存 400 份按站点归档的岗位原始文本，其中：

| 数据源       | 原始文本数量 | manifest 记录数 |
| :----------- | -----------: | --------------: |
| 人工整理样本 |          120 |               - |
| 腾讯招聘     |          100 |             100 |
| 百度招聘     |          100 |             100 |
| 美团招聘     |          100 |             100 |
| 京东招聘     |          100 |             100 |
| **合计**     |      **520** |         **520** |

### 2.2 实体抽取

#### 2.2.1 基于正则表达式的基础字段提取

#### 2.2.2 基于LLM的深度知识抽取与混合解析策略（谢羿衡）

为提高技能实体抽取的细粒度和准确性，本项目设计并实现了基于DeepSeek LLM的深度知识抽取通道，作为正则解析的增强方案。

**混合解析架构**

采用"正则预解析 + LLM深度抽取"的两阶段混合策略：

1. **阶段一：正则基础解析**\
   RegexParser 先快速提取结构化元数据（职位名称、地点、学历、工作年限、部门等），作为兜底结果。
2. **阶段二：LLM深度抽取**\
   LLMParser 调用 DeepSeek API 进行细粒度技能拆分、职责结构化和语义理解，将结果与阶段一合并。

**LLM系统提示词设计**

设计了88行的结构化系统提示词（见 [config.py:11-87](https://github.com/WHUKE/JDParser/blob/main/src/core/config.py#L11-L87)），包含：

- **JSON Schema定义**：明确17个输出字段的格式和语义
- **技能抽取10条规则**：
  - 最小粒度拆分（如"MySQL/Redis/MongoDB" → 3个独立技能）
  - 必填字段约束（name, proficiency, category, skill_type, parent）
  - 技能类型分类（hard/soft/domain/certification/language/tool）
  - 层级关系识别（parent字段，如Helm的父技能为Kubernetes）
- **字段推断规则**：job_level（根据年限和职责推断）、salary解析等
- **输出严控**：禁止schema外字段，熟练度等枚举值强制映射

**实现细节**（见 [llm_parser.py](https://github.com/WHUKE/JDParser/blob/main/src/parsers/llm_parser.py)）

```python
# 核心逻辑
def parse(self, text: str, filename: str) -> JobDescription:
    # 1. 正则解析器提取基础字段（兜底）
    jd = self._regex_parser.parse(text, filename)

    # 2. LLM深度抽取
    llm_result = self._call_llm(text)
    if llm_result:
        self.merge_extracted_result(jd, llm_result)

    return jd
```

**工程化处理**：

- **速率限制**：请求间隔0.5秒，避免触发API限流
- **错误重试**：最多3次重试，指数退避延迟
- **JSON解析容错**：处理LLM返回格式异常，降级到正则结果

**Langbase Workflow 集成**（网易LLM网关）

在网易实习期间，额外实现了基于公司内部 Langbase 平台的解析通道（见 [langbase_parser.py](https://github.com/WHUKE/JDParser/blob/main/src/parsers/langbase_parser.py)，328行）。Langbase 采用 Workflow 异步执行模式，需要更复杂的工程化处理：

1. **异步 Workflow 模式**
   - 先调用 `trigger` 接口启动任务，获取 `runID`
   - 再轮询 `workflow-runs` 接口直到任务完成（最多60轮，每轮1秒）
2. **批量并发处理**（见 [langbase_parser.py:63-123](https://github.com/WHUKE/JDParser/blob/main/src/parsers/langbase_parser.py#L63-L123)）
   - 每批并发触发5个任务（`LANGBASE_BATCH_SIZE`）
   - 批量轮询减少API调用次数，提升吞吐量
   - 保持输入输出顺序一致性
3. **智能限流重试**（见 [langbase_parser.py:200-237](https://github.com/WHUKE/JDParser/blob/main/src/parsers/langbase_parser.py#L200-L237)）
   - 触发阶段：检测 HTTP 429，使用 `Retry-After` 头指定的延迟
   - 执行阶段：解析 workflow `failed` 消息，识别上游限流（`"429" in message`）
   - 限流失败时自动重新触发任务（最多3次），避免整批失败
4. **请求速率控制**
   - 每次请求间隔1秒（`LANGBASE_REQUEST_INTERVAL`）
   - 避免短时间内大量请求触发平台限流

**批处理流程示例**：

```python
# pipeline.py 中的批处理调度
if isinstance(self._parser, LangbaseParser):
    results = self._process_directory_batch(txt_files, output_dir)

# 分3步完成一批任务
# Step 1: 批量触发（5个并发）
for text, filename in batch_items:
    run_info = self._trigger(text)
    tasks.append(TaskInfo(...))

# Step 2: 批量轮询（多任务并行等待）
while pending_tasks:
    for task in pending_tasks:
        status = check_status(task.run_id)
        if status == "success": merge_result()
        elif status == "failed" and is_rate_limit: retry()

# Step 3: 收集结果并写入文件
```

**效果对比**：

| 解析模式       | 请求方式              | 处理50个JD耗时     | 限流处理              |
| -------------- | --------------------- | ------------------ | --------------------- |
| LLM (DeepSeek) | 同步API调用           | \~30秒（0.5s间隔） | 指数退避重试          |
| Langbase       | 异步Workflow + 批处理 | \~80秒（包含轮询） | 智能识别 + 自动重触发 |

Langbase 模式虽然单次耗时更长（异步轮询开销），但支持批量并发和细粒度限流处理，适合大规模数据解析场景。

**效果**

相比纯正则解析，LLM模式能够：

- 识别隐含技能（如"分布式架构经验" → 提取"分布式系统""微服务架构"等）
- 准确区分必需技能与加分技能
- 推断技能熟练度和层级关系
- 结构化拆解职责描述

####

### 2.3 关系抽取

#### 2.3.1 技能归一化与知识融合（谢羿衡）

为确保同一技能在知识图谱中只对应唯一节点，避免别名导致的实体冗余和关系混乱，开发了完整的技能归一化模块（见 [normalizer.py](D:\Code\WHUKE\JDParser\src\core\normalizer.py)，465行代码）。

**归一化规则库**

构建了三层归一化规则体系（见 [normalizer.py](https://github.com/WHUKE/JDParser/blob/main/src/core/normalizer.py)）：

1. **技能别名映射表**（168条规则）\
   将各种缩写、俚语、版本号统一到规范名称：
   - 编程语言：`js/es6/es6+` → `JavaScript`，`k8s` → `Kubernetes`
   - 框架：`vue2/vue3/vue.js` → `Vue.js`，`springboot` → `Spring Boot`
   - 数据库：`mongo` → `MongoDB`，`postgres` → `PostgreSQL`
   - 云平台：`阿里云/aliyun` → `阿里云`，`gcp` → `Google Cloud`
   - 概念：`微服务` → `微服务架构`，`分布式` → `分布式系统`
2. **类别纠错映射表**（131条规则）\
   修正LLM可能误分类到"其他"的技能，补充正确的category和skill_type：
   ```python
   # 示例规则
   "html": ("编程语言", "hard"),
   "webpack": ("构建工具", "hard"),
   "docker": ("容器与编排", "hard"),
   "沟通能力": ("软技能", "soft"),
   "cet-6": ("通用技能", "certification"),
   ```
3. **熟练度数值映射**\
   将文本熟练度标准化为可比较的数值排名：
   ```
   了解=1, 熟悉=2, 熟练=3, 精通=4, 不限/其他=None
   ```

**归一化流程**（见 `normalize_skills()` 函数）

```python
def normalize_skills(skills: list[Skill]) -> list[Skill]:
    for skill in skills:
        # 1. 名称归一化（别名 → 规范名）
        skill.name = normalize_skill_name(skill.name)

        # 2. 类别纠错（修正category和skill_type）
        fix_skill_category(skill)

        # 3. 熟练度映射（文本 → 数值）
        skill.proficiency_rank = compute_proficiency_rank(skill.proficiency)

        # 4. 去重（同名技能只保留首次出现）
    return result
```

**工程价值**

- **减少图谱冗余**：168条别名规则确保"JavaScript"不会因为"js""JS""es6"等写法产生4个节点
- **提高查询召回**：用户搜索"K8S"时能关联到所有标记为"Kubernetes"的岗位
- **支持技能聚合**：归一化后的`proficiency_rank`可直接用于技能熟练度统计和排序

**扩展性设计**

规则表采用字典结构，新增别名只需添加一行映射，无需修改代码逻辑。类别纠错表独立维护，可持续迭代优化LLM分类误差。

####

### 2.4 其他

#### 2.4.1 增量解析文件交付与数据加载器（谢羿衡）

为支持下游数据清洗和图谱导入模块的灵活调用，设计了增量文件输出机制和标准化加载接口。

**增量文件输出设计**（见 [pipeline.py:79-86](https://github.com/WHUKE/JDParser/blob/main/src/pipeline.py#L79-L86)）

每次解析后同时生成：

1. **单文件结果**：`<原文件名>.json`（如 `xzl_16.json`）
   - 一个JD对应一个JSON文件，便于溯源和差异对比
   - 支持增量更新：重新解析单个JD只覆盖对应文件，不影响其他结果
2. **全量汇总文件**：`_all.json`
   - 数组格式，包含所有JD的解析结果
   - 便于批量导入和全量统计

**数据加载器实现**（见 [loader.py](https://github.com/WHUKE/JDParser/blob/main/src/loader/loader.py)）

提供Python API供下游模块调用：

```python
from src.loader import load_all, load_file

# 加载全量数据
jds = load_all()  # 读取 _all.json

# 加载指定文件
jds = load_file("xzl_16.json")  # 支持单对象或数组格式

# 直接访问结构化字段
for jd in jds:
    print(jd.job_title, jd.location)
    for skill in jd.required_skills:
        print(skill.name, skill.proficiency_rank)
```

**CLI工具支持**（见 [load.py](https://github.com/WHUKE/JDParser/blob/main/src/cli/load.py)）

```bash
# 列出所有解析结果
python -m src.cli.load --list

# 查看全量数据摘要
python -m src.cli.load --all

# 查看单个文件详情
python -m src.cli.load --file xzl_16.json -v
```

输出示例：

```
──────────────────────────────────────────────────────────────────
  共 50 条 JD
──────────────────────────────────────────────────────────────────
  [  1] 后端开发工程师              来源: jd_001.txt     必需技能: 15  加分技能:  5
       📍 北京  本科  3-5年
       🔧 必需: Java, Spring Boot, MySQL, Redis, Kafka, Docker, Kubernetes, Git
       ⭐ 加分: Go, ElasticSearch, Prometheus ...等5项
```

**对下游模块的价值**

- **JDGraphBuilder**：通过 `load_all()` 批量加载数据进行清洗和校验
- **Neo4j导入**：增量文件支持失败重试，无需重新解析整个数据集
- **数据分析**：标准化的JSON Schema便于统计和可视化

#### 2.4.2 质量保障与文档维护（谢羿衡）

**单元测试体系**

编写11个测试模块，共1113行测试代码，覆盖：

- 核心模块测试：`test_models.py`、`test_normalizer.py`、`test_pipeline.py`
- 解析器测试：`test_base_parser.py`、`test_regex_parser.py`
- 爬虫模块测试：`test_crawler_jd.py`、`test_crawler_baidu.py`、`test_crawler_tencent.py`、`test_crawler_meituan.py`
- 加载器测试：`test_loader.py`

测试用例示例：

```python
# 技能归一化测试
def test_normalize_skill_name():
    assert normalize_skill_name("js") == "JavaScript"
    assert normalize_skill_name("K8S") == "Kubernetes"
    assert normalize_skill_name("Vue3") == "Vue.js"

# 类别纠错测试
def test_fix_skill_category():
    skill = Skill(name="Docker", category="其他")
    fix_skill_category(skill)
    assert skill.category == "容器与编排"
    assert skill.skill_type == "hard"
```

**文档迭代**

维护项目文档（[README.md](https://github.com/WHUKE/JDParser/blob/main/README.md)），包含：

- 项目结构说明
- 快速开始指南（安装、配置、运行）
- 三种解析模式使用方法（regex/llm/langbase）
- API使用示例
- 字段说明表格
- 技能归一化规则说明

文档采用markdown格式，配合代码示例和表格，便于新成员快速上手。

---

**个人工作总结**

本人在 JDParser 模块中主要负责以下工作：

1. **技能归一化体系**（465行代码）
   - 构建168条别名映射规则 + 131条类别纠错规则
   - 实现去重、熟练度映射、层级关系处理
   - 保障知识图谱实体唯一性
2. **LLM深度解析通道**（403行核心代码 + 88行提示词）
   - 设计混合解析架构（正则兜底 + LLM增强）
   - **DeepSeek集成**：实现同步API调用，速率限制、重试、容错（75行）
   - **Langbase集成**：实现异步Workflow模式，批量并发处理、智能限流重试（328行）
   - 设计结构化系统提示词，支持细粒度技能抽取和层级关系识别
3. **数据交付与工程化**
   - 增量文件输出机制（单文件 + 全量汇总）
   - 标准化数据加载器（Python API + CLI工具）
   - 11个测试模块，1113行测试代码
   - 项目文档编写与维护

**技术亮点**：

- Langbase异步批处理：5任务/批并发，智能识别限流429并自动重触发
- 归一化规则体系：299条规则覆盖主流技术栈，支持持续扩展
- 混合解析策略：正则快速兜底 + LLM细粒度增强，保障解析成功率

**代码贡献**：源代码约1270行（normalizer.py 465行 + langbase_parser.py 328行 + llm_parser.py 75行 + base.py合并逻辑86行 + loader.py 135行 + load.py 145行 + pipeline.py批处理逻辑36行），测试代码1113行，文档154行，合计约2500+行。

### 2.5 知识图谱的规模、存储和可视化结果

#### 2.5.1 图谱可视化数据层与后端接口（邢泽雷）

本模块的后端图谱聚合逻辑位于 `JDGraphBuilder` 目录下，主要涵盖了图查询层与 API 接口服务层（源码见 [graph_query.py](../JDGraphBuilder/src/query/graph_query.py) 和 [serve.py](../JDGraphBuilder/src/cli/serve.py)）。其主要功能是针对图谱中繁杂的节点网络和海量边界进行实时聚合与图遍历降采样提取，从而为前端应用提供图谱可视化（如 D3.js 渲染所需的力导向基础节点关系）所需的子图结构。目前这一部分的代码已被删除，采用了云端部署的方式。

**核心子图网络聚合逻辑（`get_visual_graph`）**：

1. **起手种子选择与图关联发散**：将前端传入的检索关键词 `focus_skill`（如"Kubernetes"）作为网络中心种子节点发散；若无查询词，默认调取全库内排名前列的热门核心关键技能节点作为图网络大盘展示。
2. **关系维度网发散图层**：
   - 首先利用 `get_related_skills` 将与目标技能构建强共现且协同度高的周边技能节点（关联边种类为 `CO_OCCURS_WITH`）拉取，组装至返回数据网。
   - 随后再调起 `find_jobs_by_skills` 方法寻找与技能高度绑定的工作岗位群（`role` 型节点），并且以岗位为新介点跨维度辐射出关联地点（`location`）、学历属性要求（`education`）、岗位方向分类属性（`category`），这些属性分别映射为图模型中 `LOCATED_IN`、`REQUIRES_EDUCATION` 等具象化结构关联边。
3. **前端渲染友好输出适配**：该模块聚合得到的图结果数据中，为了保证前显不会呈现重复组件，内部利用节点特征名 ID 主键实现了映射去重。在响应组装时，将其转化为高度友好的包含 `{ "nodes": [...], "edges": [...] }` 标准字典数组格式，同时携带包含了当前连接边点统计总量的 `meta` 特量结构返回，最小化减轻前端组件二次解包的数据处理代价。

**轻量化接口引擎设计**：
使用原生无依赖的 `http.server.ThreadingHTTPServer` 搭建了名为 `JDGraphBuilderAPI` 的纯净微后端网络架构：

- 设计并接管暴露下端的 `/api/graph` 结构作为可视化功能引擎服务端点，能实时读取并在层接结构时利用参数（如把拉取发散规模控制的 `related_skill_limit` 值 与 岗位密度极限 `jobs_per_skill`）过滤数据，确保图网络信息量不至发生前台雪崩。
- 配置了包含 HTTP 头对跨域 `CORS` 等交互许可规则支持机制，同时为系统添加了微路由拦截模块：以满足分发 `/front/*` 以及全根路径的静止资产响应拦截，达到服务网端可以直接实现挂载渲染和托管本 Web HTML 项目轻量化全息发出的能力。

#### 2.5.2 可视化交互面板与前端界面设计（邢泽雷）

前端界面与系统的交互承接端为自行组建并构想布局命名的产品页—— **Woodle AI 知识探索端**（代码存放于项目下的 [woodle_ai_home.html](../JDGraphMono/front/woodle_ai_home.html)）。本页体使用免去包组件依赖特性的纯极简单文件驱动（HTML框架辅搭配 CSS 艺术结构）构造出犹如当代无瑕搜索引擎的高定交互质感。

**界面美学形态拟态流体设计特质**：

1. **复刻引擎核心布局结构感**：核心置中了具有彩色四种基源色拟形搭配的重绘 Logo ("Woodle")。搭配拥有巨大内距及输入提示的胶囊椭圆化主控搜索域（`.search-box` 的 CSS 应用流体特征），赋予产品本身干净及初见的通透空间感。
2. **高磨砂玻璃拟态组件阴影特效**：为了弱化图框死板特征，背景通过采用 `radial-gradient` 内扩型网格流质混合渐变体系以及利用包含玻璃透白遮罩（复合 `backdrop-filter: none` 或高阶透窗控制层）在视差中产生清透立体悬浮关系。例如聚焦反馈或系统快捷推荐点击时（芯片流 `.chip` 组件模块），引入触发伴随 140ms \~ 160毫秒过渡时间动画（`transition: transform` 与重影阴影），赋予物理悬空级向上抬托运动（`transform: translateY(-1px)`），大幅强化操作回弹力反馈感及系统流畅阻尼响应表现。
3. **系统视图无感态解耦结构变型架构**：基于轻量化构建宗旨，未采用三方路由。将页面分装为第一交互承接态的搜索引擎体及深延拓展使用的宽幅全景图探针结构渲染阵地面体（`.graph-panel`）两大区块。在需要发生转进流程时，由 CSS原生 `play/none` 特殊选择器类态 `.active` 的推加，隐式处理状态过渡流向操作无缝展示。

**定制全端级图可视化底座（Graph Workspace Panel）**：
知识网络进行查询唤起后的大尺寸视野控制操作版展示：

1. **多向交互图动态操作杆集**：面板顶部具备对引擎的调节条阵列（`.control-input` 控件），用来方便探索者直接使用拉杆等数值修改，实时影响触发查询传导规模及发散强度的极限设定调节；外加侧方提供了多组命令交互功能。
2. **沉浸环境底图区**：在内部留有的挂载全屏力引向渲染图及 SVG 对象图的大画布外，为其搭建防重叠隔离。在其周边提供（`.graph-status`），可以执行例如监测返回时间及因为错误超时引发红牌断供等展示容灾及友好的弹性回落降级状态报错体系体验。

---

**个人工作总结**

本人在图谱可视化实现与前端交互设计模块中主要负责以下工作：

1. **构建图可视化数据引擎与服务端接口**
   - 设计并实现了图谱分析结果到前端可视化组件的数据转译层（`graph_query.py`），提供基于种子扩展的强降采样与节点过滤策略（`get_visual_graph`），防止大型连通图引起前端雪崩。
   - 脱离重量级第三方 Web 框架依赖，完全基于 Python 自带的 `http.server` 模块，搭建了包含资产托管拦截、`CORS` 跨域处理与微路由在内的轻量 API 服务（`serve.py`）。
2. **Woodle AI 前端交互与纯原生 SPA 底座开发**
   - 独立构思并设计了 “Woodle AI” 极简高态搜索引擎级视图交互，使用了原生 HTML 与流体 CSS 动画（如利用 `backdrop-filter` 和原生 `radial-gradient` 呈现高磨砂拟态空间悬浮体系）。
   - 实现无三方路由下纯靠 CSS 选择器控制的视图平滑转场，集成动态调节配置栏台和错误自动收档重试体验的力导向渲染探针面板组件。

**技术亮点**：

- **无依赖轻量服务端架构**：未依赖 Flask / FastAPI 等大型全家桶框架，借助原生库构建稳定轻便的微后置接口处理和基于 JSON 的纯净 API 网关及静态文件映射器。
- **性能抗载聚合限流**：针对力导向图可视化数据爆炸瓶颈，使用后端过滤节点共现度（`related_skill_limit`）并收束介点岗位边界（`jobs_per_skill`）进行数据稀疏化控制，降低传输负担。
- **高拟真原生流体交互**：全部借助流式响应和 HTML API 完成，运用 140ms \~ 160ms 不等的拟态缓冲位移动画及弹性边界响应方案带来了极为丝滑现代的操作物理沉浸体验感。

**代码贡献**：源代码合计约 1150+ 行。其中前端主面板架构、渲染逻辑及艺术 CSS 动画（`woodle_ai_home.html`）约计 900+ 行，后端微型 HTTP Web API 提供程序核心架构模块（`serve.py`）约计 150 行，图结构抽取采样逻辑及映射方法（`graph_query.py`）约计 100 行。

<br />

### 2.6 数据清洗（刘义群）

为确保知识图谱数据的质量和一致性，设计并实现了完整的数据清洗模块（见 `JDGraphBuilder/src/cleaner/`），承接 JDParser输出的结构化 JSON，进行字段级清洗、校验与标准化，为后续图建模与导入提供干净、规范的数据。

**模块架构**

数据清洗模块由 7 个核心文件组成：

```
src/cleaner/
├── base.py               # 基础类定义
├── field_cleaner.py      # 字段级清洗（地点/学历/经验/职位/熟练度）
├── salary_cleaner.py     # 薪资数据清洗与标准化
├── skill_cleaner.py      # 技能数据清洗与增强
├── skill_hierarchy.py    # 技能层级规则库（父子技能推断）
├── pipeline.py           # 清洗流水线（编排全流程）
└── validator.py          # 数据校验（必填项/值域检查）
```

**核心清洗流程**

```
原始 JD JSON（来自 JDParser）
       ↓
  [validator.py] 校验必填字段与值域合法性
       ↓
  [field_cleaner.py] 字段级清洗（地点/学历/经验/类别/职位名/熟练度）
       ↓
  [salary_cleaner.py] 薪资结构化（统一为 {min, max, unit}）
       ↓
  [skill_cleaner.py] 技能去重、补全、与 skill_hierarchy 集成
       ↓
  [pipeline.py] 汇总输出 _all_cleaned.json + _cleaning_report.json
       ↓
  干净数据 → M4 图建模与导入
```

**字段级清洗（`field_cleaner.py`）**

实现了 5 大字段的归一化处理，以下是各字段清洗前后的对比示例：

#### 2.6.1. 地点归一化 `clean_location()`

**问题背景**：原始 JD 数据中地点表述不统一，存在多种格式混用：

- 带行政后缀：`"广州市"`、`"杭州市"`
- 多地点混排：`"北京、上海、深圳"`、`"北京/上海"`
- 缺失值：`None` 或空字符串

**清洗示例**：

| 原始输入             | 清洗输出                   | 说明             |
| -------------------- | -------------------------- | ---------------- |
| `"广州市"`           | `["广州"]`                 | 去除"市"后缀     |
| `"北京、上海、深圳"` | `["北京", "上海", "深圳"]` | 中文顿号分隔     |
| `"北京/上海"`        | `["北京", "上海"]`         | 斜杠分隔         |
| `"北京市朝阳区"`     | `["北京"]`                 | 去除"市""区"后缀 |
| `None`               | `["未知"]`                 | 空值兜底         |



#### 2.6.2. 学历归一化 `clean_education()`

**问题背景**：学历字段存在大量变体，包括中文口语表达和英文缩写：

- `"本科及以上"`、`"大学本科"`、`"统招本科"` 均应归一为 `"本科"`
- `"Bachelor"`、`"master"` 等英文需转换

**三级归一策略**（优先级从高到低）：

1. **精确匹配有效枚举值**（直接通过）：`["博士", "硕士", "本科", "大专", "不限"]`
2. **映射表查找**（大小写不敏感）：`"bachelor"` → `"本科"`
3. **模糊包含匹配**（兜底）：`"本科及以上"` 包含 `"本科"` → `"本科"`

**清洗示例**：

| 原始输入       | 清洗输出 | 策略         |
| -------------- | -------- | ------------ |
| `"本科及以上"` | `"本科"` | 模糊包含匹配 |
| `"bachelor"`   | `"本科"` | 映射表查找   |
| `"统招本科"`   | `"本科"` | 映射表查找   |
| `"硕士研究生"` | `"硕士"` | 模糊包含匹配 |
| `"Master"`     | `"硕士"` | 映射表查找   |
| `None`         | `"不限"` | 空值兜底     |

#### 2.6.3. 工作经验结构化 `clean_experience()`

**问题背景**：经验字段格式极度多样：

- 区间型：`"3-5年"`
- 下限型：`"3年以上"`、`"3+年"`
- 特殊型：`"应届"`、`"不限"`、`"3年左右"`
- 噪声型：`"GPU编程和模型优化"`（非经验字段内容误填）

**统一输出结构**：`{"min": int, "max": int|None}`

**清洗示例**：

| 原始输入    | 清洗输出                  | 说明           |
| ----------- | ------------------------- | -------------- |
| `"3-5年"`   | `{"min": 3, "max": 5}`    | 区间型         |
| `"3年以上"` | `{"min": 3, "max": None}` | 下限型         |
| `"3+年"`    | `{"min": 3, "max": None}` | 简写下限型     |
| `"应届"`    | `{"min": 0, "max": 0}`    | 应届生特殊处理 |
| `"3年左右"` | `{"min": 2, "max": 4}`    | 容差 ±1 处理   |
| `"不限"`    | `{"min": 0, "max": None}` | 无经验要求     |
| `"GPU编程"` | `{"min": 0, "max": None}` | 噪声兜底       |

**核心正则表达式**：

```python
_EXP_RANGE_RE = re.compile(r"(\d+)\s*[-–~]\s*(\d+)\s*年")   # "3-5年"
_EXP_MIN_RE   = re.compile(r"(\d+)\s*年")                    # "3年以上"
_EXP_PLUS_RE  = re.compile(r"(\d+)\s*\+\s*年")              # "3+年"
```

#### 2.6.4. 职位类别归一化 `clean_job_category()`

**问题背景**：不同招聘平台使用不同的类别命名体系

**清洗示例**：

| 原始输入                              | 清洗输出     |
| ------------------------------------- | ------------ |
| `"技术-服务端开发-服务端开发"`        | `"后端开发"` |
| `"后端"`                              | `"后端开发"` |
| `"程序&技术类"`                       | `"技术"`     |
| `"技术-前端及客户端开发-web前端开发"` | `"前端开发"` |
| `"数据"`                              | `"数据分析"` |

#### 2.6.5. 技能熟练度归一化 `clean_proficiency()`

**问题背景**：JD 中描述技能熟练程度的词汇极度多样，涉及程度副词、评价性形容词等 50+ 种表达。

**核心映射表**（部分示例）：

| 原始表达     | 归一化结果 | 类别         |
| ------------ | ---------- | ------------ |
| `"精通"`     | `"精通"`   | 标准五级     |
| `"扎实"`     | `"熟练"`   | 程度副词     |
| `"优秀"`     | `"精通"`   | 评价性形容词 |
| `"有经验"`   | `"熟练"`   | 经验描述     |
| `"初级"`     | `"了解"`   | 等级标称     |
| `"熟练掌握"` | `"熟练"`   | 复合动词     |
| `"深入理解"` | `"精通"`   | 程度副词     |

**未识别词处理**：当遇到映射表未收录的词时，系统记录到计数器并在清洗报告中呈现，供下一轮回填

#### 2.6.6. 薪资清洗模块（`salary_cleaner.py`）

**问题背景**：薪资是知识图谱中支撑"技能稀缺度分析"的关键字段，但原始数据中薪资格式极度多样：

- 结构化：`salary_min=15, salary_max=30, salary_unit="K/月"`
- 纯文本：`"15-30K·15薪"`、`"月薪 15000-30000 元"`、`"年薪 20-40 万"`

**统一输出格式**：`{"min": float, "max": float, "unit": "月"|"年"}`，所有月薪换算为 **K 为单位**。

**清洗示例**：

| 输入类型 | 原始输入                                           | 清洗输出                                   | 说明       |
| -------- | -------------------------------------------------- | ------------------------------------------ | ---------- |
| 结构化   | `salary_min=15, salary_max=30, unit="K/月"`        | `{"min": 15.0, "max": 30.0, "unit": "月"}` | 直接标准化 |
| 结构化   | `salary_min=15000, salary_max=30000, unit="元/月"` | `{"min": 15.0, "max": 30.0, "unit": "月"}` | 元转K      |
| 纯文本   | `"15-30K·15薪"`                                    | `{"min": 15.0, "max": 30.0, "unit": "月"}` | 提取K值    |
| 纯文本   | `"年薪 20-40 万"`                                  | `{"min": 20.0, "max": 40.0, "unit": "年"}` | 年薪处理   |
| 纯文本   | `"月薪 15000-30000 元"`                            | `{"min": 15.0, "max": 30.0, "unit": "月"}` | 元转K      |
| 纯文本   | `"面议"`                                           | `None`                                     | 无法解析   |

#### 2.6.7. 技能清洗模块（`skill_cleaner.py`）

技能数据是知识图谱**最核心的实体**，其质量直接影响图谱中 `PARENT_OF` 和 `CO_OCCURS_WITH` 关系的密度与准确度。

**问题背景**：从 JDParser 输出的技能数据存在以下典型问题：

| 问题类型                                | 示例                                          | 影响                             |
| --------------------------------------- | --------------------------------------------- | -------------------------------- |
| 同一技能在 required 和 preferred 中重复 | required 中有 `Java`，preferred 中也有 `Java` | 图谱中出现重复边，共现统计失真   |
| 同一列表内技能名重复                    | required 中出现两个 `Python`                  | 节点重复，导入报错               |
| proficiency 字段缺失                    | `{"name": "Redis"}` 无 proficiency            | 字段不完整，下游无法按熟练度过滤 |
| category 字段缺失                       | `{"name": "Redis"}` 无 category               | 技能归类丢失                     |
| parent 字段缺失                         | `{"name": "Spring Boot"}` 无 parent           | 无法建立 PARENT_OF 关系          |
| proficiency 为非标准值                  | `"扎实"` 不在标准五级枚举中                   | 值域不一致                       |

**三步处理流程**：

```python
def clean_skills(required: list[dict], preferred: list[dict]) -> tuple[list[dict], list[dict]]:
    # Step 1: 单技能清洗（补全缺失字段）
    required  = [_clean_single_skill(s) for s in required]
    preferred = [_clean_single_skill(s) for s in preferred]

    # Step 2: 列表内去重（大小写不敏感）
    required  = _dedup_skills(required)
    preferred = _dedup_skills(preferred)

    # Step 3: 跨列表去重（preferred 中移除已在 required 中出现的技能）
    required_names = {s["name"].lower() for s in required}
    preferred = [s for s in preferred if s["name"].lower() not in required_names]

    return required, preferred
```

**字段补全策略**：

| 字段          | 缺失时的默认值 | 归一化方式                   | 设计理由                             |
| ------------- | -------------- | ---------------------------- | ------------------------------------ |
| `name`        | `""` (空串)    | `.strip()` 去空白            | name 是技能唯一标识，必须有值        |
| `proficiency` | `"不限"`       | `clean_proficiency()` 归一化 | 空值意味着"未指定要求"，等同于"不限" |
| `category`    | `"其他"`       | 空值检测后赋默认             | 无分类信息的技能归入"其他"，不丢弃   |
| `parent`      | `None`         | `infer_parent()` 规则推断    | **优先保留已有值**，规则推断只作兜底 |



#### 2.6.8. 技能层级规则库（`skill_hierarchy.py`）

**功能定位**：为技能节点补全 `parent` 字段，是知识图谱中 `PARENT_OF` 关系自动构建的核心支撑。没有此模块，无法为 `Spring Boot → Spring` 这样的技能建立层级关系，用户也就无法通过"展开技能树"来浏览技能体系。

**问题背景**：在原始 JD 数据中，技能的 `parent` 字段常常缺失，原因包括：

- M2 的规则解析器不输出 `parent` 字段（正则无法推断从属关系）
- 即使使用 LLM 解析，也可能遗漏或给出错误的 `parent`
- 部分技能的从属关系是隐含的业界常识（如 `Docker Compose` 是 `Docker` 的编排工具），原数据不会显式标注

**规则设计原则**：

| 原则                                 | 说明                                   | 正例                                               | 反例                                 |
| ------------------------------------ | -------------------------------------- | -------------------------------------------------- | ------------------------------------ |
| 只收录业界广泛认可的从属关系         | 需在业界文档/社区中有明确的"属于"描述  | `Spring Boot` → `Spring`                           | `Vite` → `Vue.js`（Vite 是独立工具） |
| 避免把"同一生态的平行技能"误判为父子 | 并列关系的技能不应建立层级             | —                                                  | `Redis` → `MySQL`（均为数据库）      |
| 所有键值使用标准化后的规范名称       | 与 normalizer 输出一致                 | `"Vue.js"` 而非 `"vue"`                            | —                                    |
| 允许嵌套层级                         | 父技能本身也可以是另一个父技能的子技能 | `Spring Cloud Alibaba` → `Spring Cloud` → `Spring` | —                                    |

**parent 推断示例**：

| 技能名称               | 原始 parent          | 推断后 parent        | 说明               |
| ---------------------- | -------------------- | -------------------- | ------------------ |
| `Spring Boot`          | `None`               | `Spring`             | 规则匹配           |
| `Spring Boot`          | `"Spring Framework"` | `"Spring Framework"` | 保留已有值，不覆盖 |
| `Spring Cloud Alibaba` | `None`               | `Spring Cloud`       | 二级父技能（嵌套） |
| `Helm`                 | `None`               | `Kubernetes`         | 规则匹配           |
| `Vue Router`           | `"Vue"`              | `"Vue"`              | 保留已有值         |
| `Java`                 | `None`               | `None`               | 无规则匹配         |

**反向索引自动构建**：

规则表以正向形式维护，但查询时需要从子技能名反查父技能名。因此在模块加载时一次性构建反向索引：

```python
def _build_child_to_parent() -> dict[str, str]:
    mapping: dict[str, str] = {}
    for parent, children in PARENT_RULES.items():
        for child in children:
            key = child.strip().lower()  # 统一小写，支持大小写不敏感查询
            if key and key not in mapping:
                mapping[key] = parent
    return mapping

# 模块级缓存：import 时即构建，后续查询 O(1)
_CHILD_TO_PARENT: dict[str, str] = _build_child_to_parent()
```

构建后的反向索引示例：

```python
_CHILD_TO_PARENT = {
    "spring boot": "Spring",
    "spring cloud": "Spring",
    "spring cloud alibaba": "Spring Cloud",  # 嵌套：二级父技能
    "vue router": "Vue.js",
    "docker compose": "Docker",
    "django": "Python",
    "typescript": "JavaScript",  # TS 是 JS 的超集
}
```

#### 2.6.9. 数据校验模块（`validator.py`）

**功能定位**：在清洗流程的**最前端**运行，负责对 M2 输出的原始 JD 数据进行质量检查。核心设计原则是**只记录问题，不中断流程**——即使数据存在问题，清洗流水线仍然会继续执行，确保数据不会因个别字段缺失而整条丢弃。

**问题背景**：从 JDParser 输出的数据质量参差不齐，常见问题包括：

- 缺少 `source_file`（无法追溯来源）
- 缺少 `job_title`、`location` 等关键字段（影响图谱节点创建）
- 学历值不在标准枚举中（如 `"985本科"`）
- 技能的 proficiency 值不在枚举中（如 `"扎实"`、`"掌握"`）

**校验常量定义**：

```python
# src/config.py - 全项目统一的值域定义
VALID_EDUCATION = ["博士", "硕士", "本科", "大专", "不限"]
VALID_PROFICIENCY = ["了解", "熟悉", "熟练", "精通", "不限"]

# src/cleaner/validator.py
REQUIRED_FIELDS = ["source_file"]  # 必填字段
WARN_IF_MISSING = ["job_title", "location", "education", "job_category"]  # 建议字段
```

**四级校验体系**：

| 级别  | 检查内容               | 日志级别  | 示例警告消息                                       |
| ----- | ---------------------- | --------- | -------------------------------------------------- |
| 第1级 | 必填字段缺失           | `ERROR`   | `[test_01.txt] 缺少必填字段: source_file`          |
| 第2级 | 建议字段缺失           | `WARNING` | `[test_01.txt] 缺少建议字段: job_title`            |
| 第3级 | 学历值不在标准枚举     | `WARNING` | `[test_01.txt] 非标准学历值: '985本科'`            |
| 第4级 | 技能熟练度不在标准枚举 | `DEBUG`   | `[test_01.txt] 技能 'Java' 的非标准熟练度: '扎实'` |

**校验结果在清洗报告中的呈现**：

```json
{
    "warnings_by_type": {
        "missing_optional_field": 45,
        "invalid_proficiency": 23,
        "invalid_education": 3,
        "missing_required_field": 1
    },
    "warnings_by_source": {
        "boss_001.txt": ["缺少建议字段: education", ...],
        "lagou_003.txt": ["非标准学历值: '985本科'", ...]
    }
}
```

#### 2.6.10. 清洗流水线（`pipeline.py`）

**作用**：编排完整的清洗流程，将上述所有子模块串联，并生成结构化的清洗报告。

**清洗报告字段说明**：

| 字段                        | 含义                         | 示例值                                                           |
| --------------------------- | ---------------------------- | ---------------------------------------------------------------- |
| `total_jds`                 | 总处理条数                   | `120`                                                            |
| `jds_with_warnings`         | 有问题的 JD 数量             | `35`                                                             |
| `total_warnings`            | 警告总数                     | `86`                                                             |
| `warnings_by_type`          | 警告类型分布                 | `{"missing_optional_field": 45, "invalid_proficiency": 23, ...}` |
| `unknown_proficiency_terms` | 未命中映射表的熟练度词及频次 | `{"精通并熟练": 5, "有项目经验": 3, ...}`                        |
| `warnings_by_source`        | 按来源文件的详细警告列表     | `{"boss_001.txt": [...], "lagou_003.txt": [...]}`                |

**完整清洗报告示例**：

```json
{
  "total_jds": 120,
  "jds_with_warnings": 35,
  "total_warnings": 86,
  "warnings_by_type": {
    "missing_required_field": 1,
    "missing_optional_field": 45,
    "invalid_education": 3,
    "invalid_proficiency": 23,
    "other": 14
  },
  "unknown_proficiency_terms": {
    "精通并熟练": 5,
    "有项目经验": 3,
    "深度掌握": 2
  },
  "warnings_by_source": {
    "boss_001.txt": [
      "[boss_001.txt] 缺少建议字段: education",
      "[boss_001.txt] 技能 'Java' 的非标准熟练度: '扎实'"
    ],
    "lagou_003.txt": ["[lagou_003.txt] 非标准学历值: '985本科'"]
  }
}
```

#### 2.6.11. 熟练度审计工具（`scripts/audit_proficiency.py`）

**背景**：随着数据批次增加，会不断出现新的未识别熟练度词。此脚本用于定期扫描数据，发现需要回填到 `_PROFICIENCY_MAP` 的新词。

**输出示例**：

```
扫描文件: data/input/_all.json
共 120 条 JD，合计 856 个技能项带 proficiency
不同 proficiency 值总数: 47
其中未命中映射表: 3 种

============================
未识别 proficiency 值（按频次降序）
============================
  '精通并熟练'              × 5
  '有项目经验'              × 3
  '深度掌握'                × 2

============================
建议追加到 field_cleaner._PROFICIENCY_MAP（频次 >= 2）
============================
    "精通并熟练": "TODO",   # 出现 5 次
    "有项目经验": "TODO",   # 出现 3 次
    "深度掌握":   "TODO",   # 出现 2 次
```

**工程化价值**

- **数据一致性**：确保图谱中实体名称、枚举值、结构的统一
- **问题可追溯**：详细的清洗报告支持按来源定位数据质量问题
- **扩展性**：映射表采用外部 JSON 配置，可热更新无需修改代码
- **容错设计**：缺失字段自动补全默认值，非标准值自动归一化

**个人工作总结**

本人在数据清洗模块中主要负责以下工作：

1. **字段级清洗引擎**（`field_cleaner.py`）：实现地点、学历、经验、职位类别、熟练度五大字段的归一化处理，构建 50+ 种熟练度表达的映射表
2. **薪资清洗模块**（`salary_cleaner.py`）：设计双策略解析（结构化数据 + 正则文本解析），统一薪资输出格式
3. **技能清洗与去重**（`skill_cleaner.py`）：实现技能补全、列表内去重、跨列表去重的完整流程
4. **技能层级规则库**（`skill_hierarchy.py`）：构建覆盖 80+ 父子关系的规则库，支持自动推断技能层级
5. **数据校验模块**（`validator.py`）：设计四级校验体系，记录问题不中断流程
6. **清洗流水线**（`pipeline.py`）：编排完整清洗流程，生成结构化清洗报告

**技术亮点**：

- **多层归一化策略**：地点、学历、熟练度均采用多级映射策略，确保高覆盖率和准确性
- **智能去重逻辑**：先补全后去重、先列表内后跨列表的执行顺序，确保数据完整性和一致性
- **规则库反向索引**：模块加载时自动构建子→父的反向索引，查询效率 O(1)
- **渐进式校验体系**：四级校验分级处理，区分严重程度，避免日志泛滥

**代码贡献**：源代码约 900+ 行（field_cleaner.py 300+ 行 + salary_cleaner.py 150+ 行 + skill_cleaner.py 50+ 行 + skill_hierarchy.py 200+ 行 + validator.py 50+ 行 + pipeline.py 150+ 行）。

## 3 基于知识图谱的应用

### 3.1 系统的基本功能

### 3.2 知识图谱的实现细节
